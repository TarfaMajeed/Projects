# TripAdvisor Project

Full-stack web application built with React (Vite) + Node.js/Express.

## Project Structure
- `/client` - Frontend (React)
- `/server` - Backend (Node.js)

## How to Run

### 1. Install dependencies
```bash
npm run install:all